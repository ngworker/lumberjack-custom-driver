import { InjectionToken } from '@angular/core';

import { <name-capitalize-united>InternalConfig } from './<name-hyphen>-internal.config';

export const <name-camel>ConfigToken: InjectionToken<<name-capitalize-united>InternalConfig> = new InjectionToken(
  '__<name-uppercase-underscore>_CONFIG__'
);
